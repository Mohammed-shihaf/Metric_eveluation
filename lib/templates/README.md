# Language templates (future)

Add `lib/templates/<language>/` skeletons here when implementing non-Python branches.
The generator interface in `lib/generator.py` is already parameterized by `(technique, metric, language, version)`.
